import { type LucideIcon, Loader2, ChromeIcon as Google } from "lucide-react"

interface Icons {
  spinner: LucideIcon
  google: LucideIcon
}

export const Icons: Icons = {
  spinner: Loader2,
  google: Google,
}

